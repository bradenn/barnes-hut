for(let i = -256; i < 256; i++){
	for(let j = -256; j < 256; j++) {
		let a = 1024, b = 1024, c = 1024;
		let v = 2E-4;
		let d = 1000;
		console.log(`${1 - Math.random() * 2} ${1 - Math.random() * 2} ${1 - Math.random() * 2} 0 0 0 ${Math.random() * d}`);
	}
}
